#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../redux-i18n/bin/i18n_extract.js" "$@"
  ret=$?
else 
  node  "$basedir/../redux-i18n/bin/i18n_extract.js" "$@"
  ret=$?
fi
exit $ret
